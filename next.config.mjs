/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["digital-affinity.s3.us-east-1.amazonaws.com", "lh3.googleusercontent.com"]
    }
};

export default nextConfig;
